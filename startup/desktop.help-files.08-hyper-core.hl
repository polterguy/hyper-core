/*
 * Creates our help file Active Event sink, which returns the main root folder for Hyper IDE's
 * help files to caller.
 */
create-event:desktop.help-files.08-hyper-core

  return:@HYPER-CORE/help-files/Hyper Core/
